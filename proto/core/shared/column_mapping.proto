syntax = "proto3";

package core.shared;

// ColumnMapping specifies how columns from a source table connect
// to columns in a target table to form a relation or foreign key.
message ColumnMapping {
  // The local column name in the source table.
  string source_column = 1;

  // The referenced column name in the target table.
  string target_column = 2;
}
